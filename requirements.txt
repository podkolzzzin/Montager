# Core dependencies
opencv-python>=4.8.0
mediapipe>=0.10.0
numpy>=1.24.0
soundfile>=0.12.0
scikit-learn>=1.3.0

# PyTorch CPU-only (for Silero VAD)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchaudio>=2.0.0

# GUI
PyQt5>=5.15.0

# Optional: better diarization (adds ~500MB, not included in builds)
# pip install pyannote.audio>=3.0.0
